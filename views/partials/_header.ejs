<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav" style="margin: 1em">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
          <% if(!user){ %>
            <a class="nav-item nav-link text-right" href="/login">Login</a>
            <a class="nav-item nav-link text-right" href="/register">Register</a>
            </form>
          <% } else if (user.email) { %>
            <form class="form-inline" action="/logout" method="POST">
            <label class = "pr-2 ml-5">Logged in as: <%= user.email %></label>
            <button type="submit" class="btn btn-outline-dark">Log Out</button>
            </form>
         <% } %>
      </div>
    </div>
  </nav>
</header>